version: '3.8'
services:
  devcontainer:
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../..:/workspaces:cached
      - nix:/nix
    command: sleep infinity

volumes:
  nix:
    name: nix-store